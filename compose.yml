services:
  hrp-dashboard:
    build: .
    container_name: hrp_dashboard
    restart: always
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:rw 
      # Mount data dir so it persists on host and avoids file-vs-dir issues
